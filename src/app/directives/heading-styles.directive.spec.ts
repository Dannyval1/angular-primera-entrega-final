import { HeadingStylesDirective } from './heading-styles.directive';

describe('HeadingStylesDirective', () => {
  it('should create an instance', () => {
    const directive = new HeadingStylesDirective();
    expect(directive).toBeTruthy();
  });
});
